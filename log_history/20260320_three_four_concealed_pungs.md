# PROGRESS.md

任務：實作「三暗刻」與「四暗刻」台數，並更新 README.md
目標檔案：mahjong.py、README.md
狀態說明：[ ] 未開始 | [/] 進行中 | [v] 完成

---

## 任務清單

### 1. [v] score_hand() 加入三暗刻／四暗刻判斷

- **檔案範圍**：`mahjong.py`（`score_hand()` 函式，手型台數區塊）
- **摘要**：
  - 在 `score_hand()` 中，針對 `hand_all`（含胡牌那張牌）計算
    每個牌面種類（`t // COPIES`）的出現次數
  - `concealed_pungs = sum(1 for c in counts.values() if c >= 3)`
  - 若 `concealed_pungs >= 4` 且 `not has_meld`（無任何明牌）：
    加 `("四暗刻", 5)`，同時**跳過**三暗刻以避免重複計台
  - 否則若 `concealed_pungs >= 3`：加 `("三暗刻", 2)`
- **台數依據**：三暗刻 +2 台；四暗刻 +5 台
- **驗收**：手動建構 3 種同三張的 hand_all → 台數出現「三暗刻+2」；
    全暗 4 種同三張 + 1 對 → 出現「四暗刻+5」
- **17132a5** feat(mahjong-py): 實作三暗刻與四暗刻台數判斷

### 2. [v] README.md 新增胡牌台數說明章節

- **檔案範圍**：`README.md`（新增「## 胡牌台數」小節）
- **摘要**：
  - 新增完整台數規則表格，列出目前所有已實作的台數項目與值：
    搶槓+1、槓上開花+1、莊家+1、拉莊+N、自摸/不求/門清、
    花牌+1/張、自風+1、圈風+1、三元牌+1、花槓+2、
    清一色+8、混一色+4、碰碰胡+4、三暗刻+2、四暗刻+5
- **驗收**：README 新增章節，格式為 Markdown 表格
- **947efed** docs(README): 新增胡牌台數章節，列出全部已實作台數規則

---

## 狀態更新規則
- [ ] 未開始
- [/] 進行中
- [v] 完成（完成後附 Commit 編號前7碼與訊息摘要）
